apiVersion: v1
kind: Service
metadata:
  name: my-svc-nodeport-1
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: catalog
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30007
